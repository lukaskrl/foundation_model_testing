# Default training/eval env (everything except STU-Net).
# Python 3.10+ recommended.
torch>=2.2
monai>=1.3,<1.5
nibabel>=5.1
numpy>=1.24
pyyaml>=6.0
tqdm>=4.66
wandb>=0.16

# Per-model extras (uncomment depending on which adapters you use):
# einops>=0.7              # 3DINO, CT-CLIP
# timm>=0.9                # 3DINO
# xformers>=0.0.23         # 3DINO (optional fast attention)
# transformers>=4.40       # BiomedParse text branch (we discard it but the import chain may need it)
# open-clip-torch>=2.24    # BiomedParse vision-language path
# vector-quantize-pytorch==1.1.2  # CT-CLIP / CTViT
