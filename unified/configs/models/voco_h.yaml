model:
  name: voco_h
  weights: /store/home/skrljl/projects/foundation_models/weights/VoCo/VoComni_H.pt
  preprocessing:
    # Same VoCo pretraining window as voco_b — see voco_b.yaml.
    intensity:
      a_min: -175.0
      a_max: 250.0
      b_min: 0.0
      b_max: 1.0
      clip: true
  kwargs:
    feature_size: 192
    use_v2: true
    in_chans: 1
